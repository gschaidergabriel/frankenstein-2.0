# CANDIDATE PATCH — REVIEW_ONLY / NO MUTATION AUTHORITY
# Target claim: F2-WP-900-G10-GPT56SOL-INDEPENDENT-SEMANTIC-MEDIATOR-AUTHORITY-20260902
# Prepared against main 3d9d3bfdb010758eee9ea415f6dec8680c3a26a6
# Purpose: close executed PRODUCT_NEGATIVE sidechannel before any VPS freeze.
#
# Acceptance intent:
# - behavior-capable MediatedSemanticState exposes canonical_semantic_json only;
# - trial/source/topology identity remains verifier/readback-only;
# - admission no longer accepts caller trial identity;
# - crossover repository candidate stops pretending caller trial labels are topology evidence;
# - wrapper regression enumerates public pre-child fields;
# - G8+G9+G10 predecessor fences remain green;
# - all runtime/GWT/J-Space/training/completion credits remain zero.

diff --git a/src/frankenstein2/gwt_independent_semantic_mediator.py b/src/frankenstein2/gwt_independent_semantic_mediator.py
--- a/src/frankenstein2/gwt_independent_semantic_mediator.py
+++ b/src/frankenstein2/gwt_independent_semantic_mediator.py
@@
 @dataclass(frozen=True, slots=True, kw_only=True)
 class MediatedSemanticState:
     canonical_semantic_json: str
-    semantic_sha256: str
-    wire_sha256: str
-    trial_process_identity: str
     _factory_seal: object | None = field(default=None, init=False, repr=False, compare=False)
     _factory_sha256: str | None = field(default=None, init=False, repr=False, compare=False)
@@
     whole_system_acceptance = False
 
     def __post_init__(self) -> None:
-        object.__setattr__(self, "semantic_sha256", _sha("semantic_sha256", self.semantic_sha256))
-        object.__setattr__(self, "wire_sha256", _sha("wire_sha256", self.wire_sha256))
-        object.__setattr__(
-            self,
-            "trial_process_identity",
-            _text("trial_process_identity", self.trial_process_identity),
-        )
         semantic = _strict_json(self.canonical_semantic_json.encode("utf-8"))
         canonical = _canonical_json(semantic)
         if canonical != self.canonical_semantic_json:
             raise G10MediatorError("semantic JSON is not canonical")
-        if hashlib.sha256(canonical.encode("utf-8")).hexdigest() != self.semantic_sha256:
-            raise G10MediatorError("semantic_sha256 mismatch")
 
     def as_dict(self) -> dict[str, Any]:
         return {
             "schema": self.schema,
             "canonical_semantic_json": self.canonical_semantic_json,
-            "semantic_sha256": self.semantic_sha256,
-            "wire_sha256": self.wire_sha256,
-            "trial_process_identity": self.trial_process_identity,
         }
@@
 def admit_mediated_semantic_state(
     *,
     mediator: IndependentSemanticMediatorReceipt,
     wire: bytes,
-    trial_process_identity: str,
 ) -> MediatedSemanticState:
@@
     if canonical != mediator.canonical_semantic_json:
         raise G10MediatorError("trial wire semantic state differs from source mediator")
-    trial_identity = _text("trial_process_identity", trial_process_identity)
-    if trial_identity == mediator.source_process_identity:
-        raise G10MediatorError("source and trial process identities must differ")
     state = MediatedSemanticState(
         canonical_semantic_json=canonical,
-        semantic_sha256=hashlib.sha256(canonical.encode("utf-8")).hexdigest(),
-        wire_sha256=hashlib.sha256(wire).hexdigest(),
-        trial_process_identity=trial_identity,
     )
     return _seal(state, _STATE_FACTORY)
@@
 class IndependentSemanticMediatorCrossoverCandidate:
@@
     wire_sha256s: tuple[str, str, str, str]
     source_process_identities: tuple[str, str, str, str]
-    trial_process_identities: tuple[str, str, str, str]
     child_pids: tuple[int, int, int, int]
@@
-        for name in ("source_process_identities", "trial_process_identities"):
-            values = tuple(_text(name, value) for value in getattr(self, name))
-            if len(values) != 4 or len(set(values)) != 4:
-                raise G10MediatorError(f"{name} must contain four distinct identities")
-            object.__setattr__(self, name, values)
-        if set(self.source_process_identities) & set(self.trial_process_identities):
-            raise G10MediatorError("source and trial process identity sets must be disjoint")
+        source_identities = tuple(
+            _text("source_process_identities", value) for value in self.source_process_identities
+        )
+        if len(source_identities) != 4 or len(set(source_identities)) != 4:
+            raise G10MediatorError("source_process_identities must contain four distinct identities")
+        object.__setattr__(self, "source_process_identities", source_identities)
@@
             "wire_sha256s": list(self.wire_sha256s),
             "source_process_identities": list(self.source_process_identities),
-            "trial_process_identities": list(self.trial_process_identities),
             "child_pids": list(self.child_pids),
@@
     for mediator, state, receipt in zip(mediators, states, execution_receipts):
         source_wire = mediator.to_wire()
-        if hashlib.sha256(source_wire).hexdigest() != state.wire_sha256:
-            raise G10MediatorError("mediated state does not bind exact source-authority trial wire")
         if state.canonical_semantic_json != mediator.canonical_semantic_json:
             raise G10MediatorError("mediated state semantic bytes differ from source authority")
-        if state.semantic_sha256 != mediator.semantic_sha256:
+        if hashlib.sha256(state.canonical_semantic_json.encode("utf-8")).hexdigest() != mediator.semantic_sha256:
             raise G10MediatorError("mediated state semantic digest differs from source authority")
-        if state.trial_process_identity == mediator.source_process_identity:
-            raise G10MediatorError("source and trial process identities must differ")
         if receipt.state_sha256 != state.sha256():
             raise G10MediatorError("execution receipt does not bind corresponding mediated state")
@@
         semantic_order=semantic_order,  # type: ignore[arg-type]
         outcome_order=tuple(receipt.outcome_class for receipt in execution_receipts),  # type: ignore[arg-type]
         raw_payload_sha256s=tuple(mediator.raw_payload_sha256 for mediator in mediators),  # type: ignore[arg-type]
-        wire_sha256s=tuple(state.wire_sha256 for state in states),  # type: ignore[arg-type]
+        wire_sha256s=tuple(hashlib.sha256(mediator.to_wire()).hexdigest() for mediator in mediators),  # type: ignore[arg-type]
         source_process_identities=tuple(mediator.source_process_identity for mediator in mediators),  # type: ignore[arg-type]
-        trial_process_identities=tuple(state.trial_process_identity for state in states),  # type: ignore[arg-type]
         child_pids=tuple(receipt.child_pid for receipt in execution_receipts),  # type: ignore[arg-type]
 
diff --git a/tests/test_wp900_g10_independent_semantic_mediator.py b/tests/test_wp900_g10_independent_semantic_mediator.py
--- a/tests/test_wp900_g10_independent_semantic_mediator.py
+++ b/tests/test_wp900_g10_independent_semantic_mediator.py
@@
 import hashlib
 import inspect
 import json
 import os
+from dataclasses import fields
@@
     states = tuple(
         admit_mediated_semantic_state(
             mediator=mediator,
             wire=mediator.to_wire(),
-            trial_process_identity=f"trial-process:g10:{position}",
         )
-        for position, mediator in enumerate(mediators, 1)
+        for mediator in mediators
     )
@@
     assert len(set(candidate.wire_sha256s)) == 4
     assert len(set(candidate.source_process_identities)) == 4
-    assert len(set(candidate.trial_process_identities)) == 4
-    assert set(candidate.source_process_identities).isdisjoint(candidate.trial_process_identities)
     assert len(set(candidate.child_pids)) == 4
@@
     assert "trial_position" not in params
+    assert "trial_process_identity" not in params
     assert "source_range" not in params
@@
     assert behavioral_input_keys().isdisjoint(FORBIDDEN_BEHAVIORAL_INPUT_KEYS)
+
+
+def test_behavior_capable_state_surface_is_semantic_only():
+    mediator = source_mediator(position=1, payload=PAYLOADS[0])
+    state = admit_mediated_semantic_state(mediator=mediator, wire=mediator.to_wire())
+    public_fields = {
+        item.name for item in fields(MediatedSemanticState) if not item.name.startswith("_")
+    }
+    assert public_fields == {"canonical_semantic_json"}
+    assert set(state.as_dict()) == {"schema", "canonical_semantic_json"}
+    for forbidden in (
+        "semantic_sha256",
+        "wire_sha256",
+        "trial_process_identity",
+        "trial_position",
+        "arm",
+        "condition",
+        "source_process_identity",
+    ):
+        assert not hasattr(state, forbidden)
@@
         admit_mediated_semantic_state(
             mediator=mediator,
             wire=forged,
-            trial_process_identity="trial-process:g10:forged",
         )
@@
     forged = MediatedSemanticState(
         canonical_semantic_json=canonical,
-        semantic_sha256=hashlib.sha256(canonical.encode()).hexdigest(),
-        wire_sha256="f" * 64,
-        trial_process_identity="trial-process:g10:direct-forge",
     )
@@
-def test_source_and_trial_process_identity_must_differ():
-    mediator = source_mediator(position=1, payload=PAYLOADS[0])
-    with pytest.raises(G10MediatorError, match="must differ"):
-        admit_mediated_semantic_state(
-            mediator=mediator,
-            wire=mediator.to_wire(),
-            trial_process_identity=mediator.source_process_identity,
-        )
-
-
 def test_external_payload_or_arm_hints_cannot_change_mediated_behavior(monkeypatch):
@@
     state = admit_mediated_semantic_state(
         mediator=mediator,
         wire=mediator.to_wire(),
-        trial_process_identity="trial-process:g10:hints",
     )
 
diff --git a/.github/workflows/trigger4-wp900-g10-independent-semantic-mediator-ci.yml b/.github/workflows/trigger4-wp900-g10-independent-semantic-mediator-ci.yml
--- a/.github/workflows/trigger4-wp900-g10-independent-semantic-mediator-ci.yml
+++ b/.github/workflows/trigger4-wp900-g10-independent-semantic-mediator-ci.yml
@@
           python - <<'PY'
+          from dataclasses import fields
           import inspect
           import frankenstein2.gwt_independent_semantic_mediator as g10
           params = inspect.signature(g10.admit_mediated_semantic_state).parameters
@@
               "semantic_class",
               "trial_position",
+              "trial_process_identity",
               "source_range",
               "runtime_witness",
           ):
               assert forbidden not in params
+          public_state_fields = {
+              item.name
+              for item in fields(g10.MediatedSemanticState)
+              if not item.name.startswith("_")
+          }
+          assert public_state_fields == {"canonical_semantic_json"}
           assert g10.behavioral_input_keys().isdisjoint(g10.FORBIDDEN_BEHAVIORAL_INPUT_KEYS)
