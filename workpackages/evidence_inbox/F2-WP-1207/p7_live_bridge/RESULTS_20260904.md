# F2-WP-1207 P7 Live Bridge -- RESULTS (2026-09-04)

paket-1788515972573-d35ba2. Wires the P7 module
(`src/frankenstein2/grid10_p7_coordination.py`, commit a9e5272, branch
`self-integration/wp1207-p7-grid10-coordination-shadow-20260904`, not yet
merged to main) into `~/.claude/star/stern.py`'s live `hook` command --
the file that is ACTUALLY REGISTERED as the hook for every running Claude
Code session on this machine -- behind a new, default-OFF flag, via an
isolated subprocess bridge (architecture decision made by Gabriel before
this task started: bridge, not merge).

## What was touched

1. `~/.claude/star/stern.py` (NOT git-versioned, 908KB single file, live
   hook for every session on this machine). Backup taken FIRST:
   `~/.claude/star/stern.py.backup-pre-p7-bridge-20260904`
   (sha256 `f96a597f88c14f54e8bd6071c471958ae153782536ea750b71bb89dce6c7a7d8`,
   identical to the pre-edit file). Diff: `star_stern_py.diff` in this
   folder -- 81 added lines, 0 removed, one contiguous insertion block
   (new flag-check + bridge-call function before `cmd_hook()`) plus a
   ~9-line guarded call site inside the existing `Stop` branch of
   `cmd_hook()`. Nothing else in the file changed.
2. `~/frankenstein-repo/scripts/stern.py` (separate git repo, ENTIRELY
   unrelated codebase to (1) despite the same filename -- confirmed at
   task start, both files only share the DB-path-resolution convention,
   pointing at the same real `unified.db`). Added CLI subcommand
   `p7-bridge-turn` + its two backing functions. Committed + pushed on
   branch `self-integration/wp1207-p7-live-bridge-20260904` in that repo,
   commit `f7373dd`. Diff: `frankenstein_repo_stern_py.diff` in this
   folder.
3. `~/frankenstein-repo/scripts/f2wp1207_grid10_p7_coordination.py` -- new
   file, a byte-identical VENDORED copy of
   `src/frankenstein2/grid10_p7_coordination.py` @ commit a9e5272.
   sha256 `9e29f77598d59cbfc5faf266b59f7ee4fd9cc6d792cb2d84c21c13ed71fbb7ad`
   -- matches `git show a9e5272:src/frankenstein2/grid10_p7_coordination.py
   | sha256sum` exactly (verified at vendor time). Vendored rather than
   live-imported across the unmerged branch, same rationale that module's
   own docstring already gives for its own p6d_runner.py citation
   (avoid cross-repo/cross-branch live-import drift risk in a shared
   process).

## Architecture actually built

`star/stern.py` `cmd_hook()`, `Stop` branch (comment: "PHASE 17 Punkt 4 --
ein Zug = ein Stop-Ereignis" -- this is the existing house convention for
"one real turn", so the bridge call sits here, not in `UserPromptSubmit`):

```
_f2wp1207_p7_bruecke_versuchen(session_id)
  -> _f2wp1207_p7_bruecke_aktiv()?  (env STERN_F2WP1207_P7_LIVE override,
     else star_konfig f2wp1207.p7_active, default OFF)
  -> if OFF: return, zero I/O, zero subprocess spawned
  -> if ON: subprocess.run([python3, ~/frankenstein-repo/scripts/stern.py,
     "p7-bridge-turn"], input={"session_id":...}, timeout=4.0,
     capture_output=True)  -- result never read/used by the caller
```

`frankenstein-repo/scripts/stern.py p7-bridge-turn`:
```
reads {"session_id":...} from stdin
-> mints/reads turn_event_id + runtime_epoch_id via EXISTING P0-P6 infra
   already in that file (_f2wp1207_next_turn_event_id,
   _f2wp1207_runtime_epoch) -- no new counting mechanism
-> imports vendored f2wp1207_grid10_p7_coordination
-> p7mod.coordinate_real_turn(schreib_verbindung(), transition_log_path,
   RealTurn(frame_id=turn_event_id, turn_event_id, runtime_epoch_id,
   session_id))
   -- this function's OWN gate (p7_active(), same env var/star_konfig key)
      is checked a SECOND time inside coordinate_real_turn itself, so even
      if the bridge were ever called by mistake with the flag off, the P7
      module refuses on its own before touching the DB
-> prints one JSON line to stdout (never read by the caller)
```

## Point-by-point against the 11 requirements

**1. `hook` calls P7 only behind a new persistent flag, default OFF.**
HELD. Flag = `STERN_F2WP1207_P7_LIVE` env override, else star_konfig key
`f2wp1207.p7_active` (same names the vendored module itself already uses
for its OWN internal gate -- both sides of the bridge read literally the
same env var and DB key, they cannot drift apart). Never set to "1" in the
persistent star_konfig table during this task; confirmed absent
(`fetchone()` -> `None`) before, during, and after all testing.

**2. OFF = byte-identical old path, proven against the real hook.**
HELD. `diff stern.py.backup-pre-p7-bridge-20260904 stern.py` = pure
81-line addition, 0 removed/changed lines elsewhere. Ran the real
`python3 stern.py hook` with a realistic `Stop`-event JSON payload
(same shape `_selbsttest_payload()` already builds for this event) against
BOTH the edited file and the untouched backup, flag absent in both: same
exit code (0), same stdout (empty in both -- the `Stop` branch never
prints), same real-DB delta (`f2_grid10_p6d_state` rows for
`condition_key='f2wp1207_p7_live'`: 0 before, 0 after, in both cases).
Repeated 4 times against the edited file (1 direct comparison + 3 more
reps) -- all identical. No subprocess was spawned in any OFF run
(confirmed: no new session_id entries appeared in
`frankenstein-repo/scripts/f2wp1207_runtime_epochs.json`, no
`p7-bruecke`-tagged lines in `hook.log`).

**3. ON = real turn -> Proposal -> Competition -> Selection -> Broadcast ->
Uptake -> recurrent state update -> reentry prep.**
HELD. Proven end to end THROUGH THE REAL `star/stern.py` `Stop` dispatch
(not just the standalone bridge subcommand) -- see "ON-path, real hook
dispatch" below. Every stage runs inside `coordinate_real_turn()`
unmodified from the vendored module (proposals via `derive_signal`+
`proposal_score`, competition via `max(scores)`, selection = the winner,
broadcast = winner's own score, uptake = 1 for winner/0 else,
`state_update()` applies lambda/alpha/beta/gamma, persisted, and the next
call's `read_prior_state()` is the reentry mechanism -- literally the same
row it just wrote).

**4. No external Effects.** HELD (unchanged from the vendored module --
`coordinate_real_turn` only touches `f2_grid10_p6d_state` + a caller-
supplied JSONL sink, no EffectGate call anywhere in the module or the new
bridge code).

**5. No semantic cell names.** HELD (`CELLS = G1..G10`, untouched, no
naming added anywhere in the bridge code either).

**6. P7 errors must NEVER break the normal hook.** HELD, triple-layered:
(a) `_f2wp1207_p7_bruecke_versuchen()`'s entire body is one
`try/except Exception` that only ever logs and returns `None`; (b) the
`subprocess.run(...)` call inside it carries `timeout=4.0` so a hung
bridge process cannot hang the hook; (c) the call SITE in the `Stop`
branch wraps the call in its OWN `try/except Exception: pass` again
(defense in depth, per the task's explicit instruction) --  a bug inside
the bridge function itself still cannot escape. Not synthetically
triggered in this round (would have required deliberately breaking the
bridge file mid-test, judged unnecessary risk against a live hook file);
the structural guarantee is the triple wrap itself, same pattern the file
already uses everywhere else (`_microclay_schatten`, `_gw_absicht_schreiben`,
etc. -- all fail-silent by the same convention).

**7. Every state transition persisted traceably.** HELD (unchanged
property of the vendored module: one JSONL line per turn via
`append_transition_log`, one UPSERT per cell into
`f2_grid10_p6d_state` with `updated_at`/`updated_by_frame_id`).

**8. Single kill-switch reverts instantly.** HELD. Flag never set ON in
the persistent config at any point; confirmed OFF as final state (see
"Final state" below). Flipping `f2wp1207.p7_active` to `"0"`/absent (or
never touching it, as here) is suffient -- no code path exists that could
run P7 without this flag being read fresh on every call (both gates read
it live, no caching).

**9. A real turn updates P7 state AND the next real turn reuses it
(reentry).** HELD, TWICE:
  - Standalone bridge subcommand, real production `unified.db`, session
    `test-p7-bridge-on-1`: turn 1 -> `G5` wins, `broadcast=0.9824`; turn 2
    (same session) -> `G10` wins, `broadcast=0.8237`, `G5`'s carried state
    from turn 1 is what feeds turn 2's `tanh(state)` term. Real-DB rows
    written: 10 (all 10 `f2_grid10_p6d_state` rows for the reserved
    `condition_key`), 0 before -> 10 after. **Deleted immediately after
    the check below** to restore the real DB to its pre-test 0-row state
    (see "Real-DB residue" below) -- all further ON-testing moved to an
    isolated throwaway copy of the same DB (`AGENTZERO_DB` env override,
    scoped to test subprocess invocations only).
  - Full end-to-end through the REAL `star/stern.py` `Stop` hook dispatch
    (not just the bridge subcommand directly), throwaway DB copy: two
    sequential `stern.py hook` invocations, same session_id, both exit 0.
    `G8` won both turns (`won_broadcast_count=2`), final state
    `1.3718496385730345` -- only reachable if turn 2 read back turn 1's
    persisted state (`0.0` prior would give a materially different,
    computable, different number). `G2` picked up a small `-0.21`
    conflict penalty (near-tie margin rule firing) -- an unforced,
    mechanism-consistent detail, not something hand-tuned into the test.

**10. Reentry across >=2 different RuntimeEpochs.** HELD, on the
standalone bridge subcommand against an isolated throwaway DB copy (kept
off the real DB deliberately for this deeper, 4-call test). Used the
REAL, already-existing `_f2wp1207_runtime_epoch(session_id,
force_new=True)` mechanism (no hand-written DB/JSON state) via a new
`--force-new-epoch` CLI flag added ONLY to the bridge subcommand, exposed
for exactly this test:
  - turn 1: epoch `d7008a8c...` minted (`predecessor=null`). `G5` wins,
    `broadcast=0.98606037...`.
  - turn 2 (same epoch): `G4` wins, `broadcast=0.93006973...`. `G5`'s
    state after turn 2 (not the winner) = `0.7 * 0.794424 = 0.5560968...`.
  - turn 3 (`--force-new-epoch`): epoch `0dba9446...` minted,
    `predecessor_epoch_id=d7008a8c...` (correct chain, confirmed in the
    returned JSON). `G5` wins again, `broadcast=1.28910583...`.
  - turn 4 (same new epoch, no force): `G4` wins.
  - **Exact math check** (not just "a number changed"): predicted
    `G5` state after turn 3 = `0.7*0.5560968 + 0.4*1 + 0.4*1.28910583
    = 1.3049101...`; after turn 4 (not winner) =
    `0.7 * 1.3049101 = 0.9134371...`. Final DB value read back:
    `G5 = 0.9134371145355425` -- matches to full float precision. This
    is only possible if the `f2_grid10_p6d_state` row (keyed by
    `condition_key`, NOT by `runtime_epoch_id`) truly carried over the
    epoch boundary that `force_new=True` created -- i.e. reentry across
    two distinct, correctly-chained RuntimeEpochs, not just two turns in
    one epoch.

**11. Flag OFF confirmed at the end, re-verified.** HELD -- see "Final
state" below.

## Real-DB residue (requirement 8 in the task prompt / honesty clause)

One exploratory ON-path check (`test-p7-bridge-on-1`, 2 turns, standalone
bridge subcommand, env-override only -- NOT the persistent flag) ran
against the REAL production `unified.db` before the throwaway-copy
approach was adopted for everything deeper. Exact counts:
`f2_grid10_p6d_state` rows for `condition_key='f2wp1207_p7_live'`:
**0 before -> 10 after** (all ten `G1..G10` rows, since the UPSERT always
touches all ten cells once active). **Deleted immediately** via a
single `DELETE ... WHERE condition_key='f2wp1207_p7_live'`, re-verified
**0 rows** afterward. No other table was touched by this test. All
subsequent ON-path testing (the epoch-reentry proof and the end-to-end
`star/stern.py` dispatch proof) used an isolated throwaway copy of the
same `unified.db` via the `AGENTZERO_DB` env-var override (scoped only to
the test subprocess invocations, never touching the persistent
environment), specifically to avoid further real-DB writes. Both
throwaway copies were deleted after use (`/tmp/p7bridge_test`,
`/tmp/p7bridge_e2e`).

## Final state (as of the last action of this task)

- `~/.claude/star/stern.py`: edited, `python3 -m py_compile` clean,
  re-verified OFF-path 0-delta ONE MORE TIME as the last check before
  writing this note (Stop-event hook run, exit 0, empty stdout, 0 rows
  for the reserved `condition_key` in the real DB).
- Persistent `star_konfig` key `f2wp1207.p7_active`: **absent (never set)
  in the real database, confirmed by direct query as the final check.**
- Env var `STERN_F2WP1207_P7_LIVE`: not exported in any persistent shell
  profile -- only ever set inline, scoped to individual test subprocess
  invocations during this task, never left set.
- **Flag is OFF right now, in the real persistent config.**
- Backup file `~/.claude/star/stern.py.backup-pre-p7-bridge-20260904`
  left in place as rollback evidence (not deleted).
- `stern.py reconcile` was deliberately NOT run by this agent -- the task
  explicitly reserves that step for the coordinator after independent
  verification (paket-1788515972573-d35ba2).

## Not done / explicitly out of scope

- Requirement 6 (fail-closed) was verified structurally (code inspection +
  the pattern match to the file's existing fail-silent conventions) rather
  than by deliberately injecting a fault into the live hook file mid-test
  -- judged too risky against a file serving real concurrent sessions.
  If the coordinator wants an active fault-injection proof, the safest way
  is a throwaway copy of `star/stern.py` (not the live one), which was not
  set up in this round.
- No merge of `frankenstein-2.0`'s P7 branch to `main` was performed or
  requested; the vendored copy in `frankenstein-repo` is intentionally a
  frozen citation of commit `a9e5272`, not a live dependency.
