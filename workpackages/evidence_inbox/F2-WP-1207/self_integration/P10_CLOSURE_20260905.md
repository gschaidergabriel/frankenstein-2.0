# P10 closed — evidence-based state continuity with behavioral effect

Origin: Claude Code coordinator session, 2026-09-05 (continuation of
2026-09-04's P6d/P7/P8/P9/P10 work). Gabriel's closing call, documented
here per house convention (short doc, no code change).

## What P10 established

Functional stack now proven, each layer independently verified (not just
trusted from agent reports), each layer's commits real and pushed:

```
P3/P4  Identity + Runtime binding
  -> P7  Coordination (proposals/competition/selection/broadcast/uptake/
          recurrent state update)
  -> P9  Epoch lifecycle / evidence-classified reentry
  -> P10 Workspace reconstruction with proven causal influence
```

Gabriel's framing, worth keeping verbatim: "Frankenstein hat jetzt
evidenzbasierte Zustandskontinuität mit Verhaltenswirkung. Nicht bloß
'gleiche ID', sondern vergangener interner Zustand kann nach Reentry
nachweislich spätere Verarbeitung verändern."

Gold test result (coordinator's own independent rerun, pinned
`turn_event_id` for a fair comparison): reconstructed predecessor state
flows exactly into the P7 formula (attributability 100%), cold-start
fallback is byte-identical to the unmodified path when no valid reentry
record exists, and the seeded-vs-cold-start `broadcast_value` differs
measurably (Δ0.43 in the reproduced run) for the identical stimulus.

## Two gaps, closed as-is (Gabriel's explicit call, not deferred as
"still open" — this is the accepted final state for P10)

- **`open_goals_tasks`**: wired (joins `entityos_arbeitspaket`), but real
  data is sparse — almost no `f2_runtime_epoch` rows join to a paket row
  yet. **Data-poverty / upstream problem, not a P10 defect.** Will fill in
  naturally as more epochs carry paket associations; no P10 rework needed
  when that happens.
- **`last accepted external/world evidence`**: deliberately excluded
  (`EXCLUDED_FIELDS` in `f2wp1207_p10_workspace_reentry.py`, both repos).
  No real backing data source was found. Stays empty until a real,
  evidenced source exists — never filled with a plausible-looking
  placeholder.

## Status

**P10 closed.** No further P10 work scheduled. Next: P11 (Effects/action
loop) — see `f2wp1207-roadmap-p5-p13` memory and the P11 kickoff package
for scope.

Related: `RUNTIME_EPOCH_LIFECYCLE_CLOSURE_20260904.md` (P9 origin),
`STERN_DIFF_AUDIT_20260904.md` + `VENDORING_DECISION_20260904.md` (P8).
