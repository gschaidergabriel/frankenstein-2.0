name: Frankenstein 2.0 Canonical Effect Authority Bridge CI

on:
  push:
    branches: [main]
    paths:
      - 'src/frankenstein2/canonical_effect_authority_bridge.py'
      - 'src/frankenstein2/effect_invocation_correlation.py'
      - 'src/frankenstein2/effect_executor_interlock.py'
      - 'tests/test_canonical_effect_authority_bridge.py'
      - '.github/workflows/canonical-effect-authority-bridge-ci.yml'
  pull_request:
    paths:
      - 'src/frankenstein2/canonical_effect_authority_bridge.py'
      - 'src/frankenstein2/effect_invocation_correlation.py'
      - 'src/frankenstein2/effect_executor_interlock.py'
      - 'tests/test_canonical_effect_authority_bridge.py'
      - '.github/workflows/canonical-effect-authority-bridge-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canonical-effect-authority-bridge:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      PYTHONPATH: .:src
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile canonical-authority bridge
        run: >-
          python -m py_compile
          src/frankenstein2/canonical_effect_authority_bridge.py
          tests/test_canonical_effect_authority_bridge.py
      - name: Run canonical effect-authority falsifiers
        run: python tests/test_canonical_effect_authority_bridge.py
