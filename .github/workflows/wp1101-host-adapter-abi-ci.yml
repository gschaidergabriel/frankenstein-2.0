name: F2 WP1101 Host Adapter ABI CI

# Exact re-entry gate: absorbed lifecycle/payload contract falsifiers must run on PR and main.
on:
  push:
    branches:
      - main
    paths:
      - 'src/frankenstein2/host_adapter_abi.py'
      - 'tests/test_host_adapter_abi.py'
      - 'tests/test_host_adapter_abi_contract_fences.py'
      - '.github/workflows/wp1101-host-adapter-abi-ci.yml'
  pull_request:
    paths:
      - 'src/frankenstein2/host_adapter_abi.py'
      - 'tests/test_host_adapter_abi.py'
      - 'tests/test_host_adapter_abi_contract_fences.py'
      - '.github/workflows/wp1101-host-adapter-abi-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deterministic:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile
        run: python -m py_compile src/frankenstein2/host_adapter_abi.py tests/test_host_adapter_abi.py tests/test_host_adapter_abi_contract_fences.py
      - name: Host adapter ABI canonical suite
        env:
          PYTHONPATH: src
        run: python tests/test_host_adapter_abi.py -v
      - name: Host adapter ABI contract-fence regressions
        env:
          PYTHONPATH: src
        run: python tests/test_host_adapter_abi_contract_fences.py -v
