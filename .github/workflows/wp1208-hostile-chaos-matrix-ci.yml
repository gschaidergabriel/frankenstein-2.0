name: F2 WP1208 Hostile Chaos Matrix CI

on:
  push:
    branches:
      - main
    paths:
      - 'src/frankenstein2/target_hostile_chaos_matrix.py'
      - 'src/frankenstein2/target_fault_scenarios.py'
      - 'tests/test_target_hostile_chaos_matrix.py'
      - '.github/workflows/wp1208-hostile-chaos-matrix-ci.yml'
  pull_request:
    paths:
      - 'src/frankenstein2/target_hostile_chaos_matrix.py'
      - 'src/frankenstein2/target_fault_scenarios.py'
      - 'tests/test_target_hostile_chaos_matrix.py'
      - '.github/workflows/wp1208-hostile-chaos-matrix-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deterministic:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test dependency
        run: python -m pip install pytest
      - name: Compile
        run: python -m py_compile src/frankenstein2/target_fault_scenarios.py src/frankenstein2/target_hostile_chaos_matrix.py tests/test_target_hostile_chaos_matrix.py
      - name: Hostile chaos matrix deterministic fail-closed suite
        env:
          PYTHONPATH: src
        run: python -m pytest -q tests/test_target_hostile_chaos_matrix.py
