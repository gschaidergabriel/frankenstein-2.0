{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/gschaidergabriel/frankenstein-2.0/schemas/EVIDENCE_SPINE.schema.json",
  "title": "Frankenstein 2.0 Telemetry Evidence Spine v1",
  "description": "Typed telemetry/hypothesis/bug records only. Immutable run-package authority is runpackages/RUN_PACKAGE_SCHEMA_V1.json and runpackages/verify_run_package.py.",
  "$defs": {
    "id": {"type": "string", "minLength": 1, "maxLength": 256},
    "timestamp": {"type": "string", "format": "date-time"},
    "telemetry_event": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "event_id", "run_id", "ts", "channel", "component", "event_type", "causal_id", "generation", "epistemic_status", "payload"],
      "properties": {
        "schema": {"const": "FRANKENSTEIN2_TELEMETRY_EVENT/v1"},
        "event_id": {"$ref": "#/$defs/id"},
        "run_id": {"$ref": "#/$defs/id"},
        "ts": {"$ref": "#/$defs/timestamp"},
        "channel": {"enum": ["SYSTEM", "COMMUNICATION", "HYPOTHESIS", "BUG", "GRID10", "PERFORMANCE", "LATENCY", "PERCEPTION", "VOICE", "EFFECT", "COMPLETION"]},
        "component": {"type": "string", "minLength": 1},
        "event_type": {"type": "string", "minLength": 1},
        "session_id": {"type": ["string", "null"]},
        "agent_id": {"type": ["string", "null"]},
        "task_id": {"type": ["string", "null"]},
        "turn_id": {"type": ["string", "null"]},
        "tool_use_id": {"type": ["string", "null"]},
        "effect_id": {"type": ["string", "null"]},
        "causal_id": {"type": "string", "minLength": 1},
        "generation": {"type": "integer", "minimum": 0},
        "epistemic_status": {"enum": ["OBSERVED", "INFERRED", "HYPOTHESIS", "SIMULATED", "UNKNOWN", "CONFLICT", "NOT_COMPUTED"]},
        "severity": {"enum": ["DEBUG", "INFO", "NOTICE", "WARNING", "ERROR", "CRITICAL"]},
        "payload": {"type": "object"},
        "provenance_refs": {"type": "array", "items": {"type": "string"}}
      }
    },
    "hypothesis_record": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "hypothesis_id", "run_id", "statement", "status", "confidence", "falsifier", "evidence_for", "evidence_against"],
      "properties": {
        "schema": {"const": "FRANKENSTEIN2_HYPOTHESIS/v1"},
        "hypothesis_id": {"$ref": "#/$defs/id"},
        "run_id": {"$ref": "#/$defs/id"},
        "statement": {"type": "string", "minLength": 1},
        "counterhypothesis_id": {"type": ["string", "null"]},
        "status": {"enum": ["OPEN", "SUPPORTED", "FALSIFIED", "CONFLICT", "DEFERRED"]},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        "falsifier": {"type": "string", "minLength": 1},
        "evidence_for": {"type": "array", "items": {"type": "string"}},
        "evidence_against": {"type": "array", "items": {"type": "string"}}
      }
    },
    "bug_record": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema", "bug_id", "status", "symptom", "root_cause_status", "root_cause_evidence", "fix_commit", "regression_test", "regression_receipt"],
      "properties": {
        "schema": {"const": "FRANKENSTEIN2_BUG/v1"},
        "bug_id": {"$ref": "#/$defs/id"},
        "status": {"enum": ["OPEN", "ROOT_CAUSE_CONFIRMED", "FIX_CANDIDATE", "REGRESSION_PENDING", "FIXED", "WONT_FIX"]},
        "symptom": {"type": "string", "minLength": 1},
        "root_cause_status": {"enum": ["UNKNOWN", "HYPOTHESIS", "CONFIRMED"]},
        "root_cause_evidence": {"type": "array", "items": {"type": "string"}},
        "fix_commit": {"type": ["string", "null"]},
        "regression_test": {"type": ["string", "null"]},
        "regression_receipt": {"type": ["string", "null"]}
      }
    }
  },
  "oneOf": [
    {"$ref": "#/$defs/telemetry_event"},
    {"$ref": "#/$defs/hypothesis_record"},
    {"$ref": "#/$defs/bug_record"}
  ]
}
